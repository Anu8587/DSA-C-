#include <bits/stdc++.h>
using namespace std;
int main(){
  //brute force
 /* int arr[] = {1,3,4,5,6,7};
  int n = sizeof(arr)/sizeof(arr[0]);

  int maxfreq = 0;
  int current;

  for (int i =0 ; i<n;++i){
    current = 1;
    for(int j = 0; j<n; ++j){
      if(i==j){
        continue;
      }
      if(arr[j] == arr[i]){
        current = current + 1;
      }
      
    }
    if (current >maxfreq){
      maxfreq=current;
    }
  }
  cout<<n-maxfreq; */

//optimized using hashing

int size(int):
x^n regt:
prices = [11,10,2,3,4]
return max pointer_to_unary_function
x=100,y=200
return xy;
x= []
}
